# Vera Certified Slice - 2026-06-25

Commit certified: `87370687202ddd95a687705d32aa6b7f19929bdd`

## What Closed

- Vault lifecycle slice from the prior commit remains in the master stack: display-once recovery codes, salted-hash-only recovery storage, wrong-key refusal, dry-run/confirmed key rotation, and Security -> Vault visibility.
- Founder command center now renders a first-paint `Founder decisions needed` lane before the slower company briefing arrives, closing the browser-rover timing gap without faking data.
- `/trust/moat` now shows governance, human-only legal/financial posture, and `Observed / Trace linked` visibility at first paint, then refreshes from live governance JSON.
- Engineering-state dirty-file reporting now preserves `git status --short` leading spaces, so visibility paths are exact.
- Truth Ledger unresolved unsupported counts now come from folded ledger state; certification-generated unsupported draft attempts are trace-linked and closed by append-only retraction events.
- `certify_new_operator_surfaces_rover` now includes governance visibility in each route row's `ok` value and records every verified operator surface file.
- `certify_expose_requires_auth` now isolates auth behavior from model/Whisper warm-up with `ANIMA_SKIP_WARM=1`, removing a full-stack timing flake while preserving product warm-up by default.
- `~/Desktop/Vera.app` is generated by checked-in `scripts/install_vera_desktop_launcher.py`; polish cert can recreate it and verify detached startup/no Terminal behavior.

## Focused Certs Run

- `scripts/certify_memory_truth_and_correction.py` - CERTIFIED
- `scripts/certify_new_surfaces_rover.py` - CERTIFIED
- `scripts/certify_new_operator_surfaces_rover.py` - CERTIFIED across 23 operator surfaces
- `scripts/certify_polish_paths.py` - CERTIFIED
- `scripts/certify_expose_requires_auth.py` - CERTIFIED
- `scripts/deploy_check.py` - GREEN on `8737068`

## Full Gate Results

- Master cert stack: `95/95 GREEN` on `8737068` in 236 seconds.
- Diamond v2: CONFIRMED with complete-per-run `[109, 109, 109]`.
- Product red: none.
- Product partial: none.
- Unclassified flakes: none.
- Honest partial: `acknowledge_flow` intentional external dependency.
- Deferred/not claimed: `audiobook_intake`.
- Enterprise scoped: `enterprise_readiness`.
- External dependency preflight: `argus` unavailable; no Diamond impact.

## Integrity Notes

- `reports/` remains ignored local evidence; the tracked proof pointer is this file plus `SYSTEM_OVERVIEW.md`.
- The live server was restarted before deploy proof and full gates; `deploy_check.py` proved git HEAD matched the running server.
- The LaCie handoff at `/Volumes/LaCie-Storage/Desktop-Folders/LBackup/2026-06-23-vera-new-mac-handoff` is an older recovery snapshot and predates this certified slice.
