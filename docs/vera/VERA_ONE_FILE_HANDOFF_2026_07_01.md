# Vera One-File Handoff - 2026-07-01

This file is the Vera-only continuity packet.

## Canonical Identity

- Product: Vera, a local-first governed AI operating system and private personality companion.
- Canonical repo: `/Users/lamarmichael/Developer/collatiolabs.com`
- Canonical GitHub home: `slmichaelj-beep/collatiolabs.com`
- Canonical branch: `anima`
- Baseline observed before this handoff file was added: `09e5daf` (`Document certified Vera hardening slice`)
- Latest certified runtime build: `87370687202ddd95a687705d32aa6b7f19929bdd`
- Verified lineage includes:
  - `09e5daf` - document certified Vera hardening slice
  - `8737068` - harden expose auth cert timing
  - `880cfa7` - stabilize visibility and live certs
  - `c04ed8f` - close vault key lifecycle
  - `aae7fd1` - add Vera new Mac handoff

Treat `anima` as the living Vera product branch. Do not create a duplicate primary Vera repo unless
Lamar explicitly decides to split Vera into its own implementation repository.

## Current Scale

- 348 Python modules under `anima/`
- 232 cert / cert-stack scripts under `scripts/`
- 37 web surfaces under `anima/web/`
- Generated `reports/` are local evidence/state and are ignored by Git.
- Sensitive runtime state lives under `.anima/` and must be treated as private.

## Broken Thread Recovery

If a Codex thread says its working directory is missing or shell startup fails, do not use that thread
for terminal-dependent verification, dashboard edits, proof ledgers, GitHub work, or generated artifacts.

Resume in a healthy thread from:

```bash
/Users/lamarmichael/Documents/Codex/2026-06-11/hey-what-s-up-i-have
```

Use the current Mac username path:

```bash
/Users/lamarmichael
```

Do not use the old migration path:

```bash
/Users/lamar
```

Verify the migration backup exists at:

```bash
/Volumes/LaCie-Storage/Desktop-Folders/LBackup/2026-06-22-codex-migration
```

Known backup note: `Desktop-Collatio` on LaCie has many more files than local `Desktop/Collatio`,
especially proofs, RTT/lean_grind, Praeco/praeco-next, and Voltus/voltus-cross. Treat that as a real
restore gap unless intentionally excluding rebuildable dependency/build trees.

Keep LaCie mounted. Do not delete the backup.

## Run Vera

```bash
cd /Users/lamarmichael/Developer/collatiolabs.com
source .venv/bin/activate
python3 -m anima.server --name Vera --port 8765
```

Open:

```text
http://127.0.0.1:8765
```

Desktop launcher:

```text
/Users/lamarmichael/Desktop/Vera.app
```

The launcher is generated by:

```bash
python3 scripts/install_vera_desktop_launcher.py
```

## Verify Vera

Before claiming status, always inspect Git state and recent commits:

```bash
cd /Users/lamarmichael/Developer/collatiolabs.com
git status --short --branch
git log --oneline -8
git remote -v
```

Deployment identity proof:

```bash
source .venv/bin/activate
python3 scripts/deploy_check.py
```

Full certification:

```bash
python3 scripts/run_master_cert_stack.py
```

Diamond repeatability:

```bash
python3 scripts/run_diamond_v2.py --gate
```

Current certified result from this branch:

- Master cert stack: `95/95 GREEN`
- Diamond v2: CONFIRMED with complete-per-run `[109, 109, 109]`
- Product red: none
- Product partial: none
- Unclassified flakes: none
- Honest partial: `acknowledge_flow`
- Deferred/not claimed: `audiobook_intake`
- Enterprise-scoped: `enterprise_readiness`
- Argus unavailable in preflight has no Diamond impact.

## Certification Standard

Respect Vera's existing cert-first pattern:

1. Build.
2. Test focused behavior.
3. Certify the behavior.
4. Run live path / browser rover where required.
5. Write report / ledger / visibility evidence.
6. Commit.
7. Restart server.
8. Run `deploy_check.py`.
9. Run master cert stack for release-safe claims.
10. Run Diamond for repeatability claims.
11. Push to `origin/anima`.

Do not claim release safety from focused certs alone.

## Core Doctrine

Vera is not primarily a revenue-ops workspace. Revenue modules exist because survival matters, but
Vera's center is:

- local-first privacy
- personal continuity
- truthful companionship
- memory and identity preservation
- governed capability
- user authority
- recoverability
- no fake green

Core boundaries:

- No unsupported claims.
- No autonomous external action.
- Financial, legal, account, tax, identity, payment, CAPTCHA, security-answer, and final-signature
  actions are human-only.
- Vera may prepare, draft, queue, track, and report.
- A human must submit, send, spend, open accounts, hold credentials, and perform legal/financial acts.
- Do not submit externally without exact action-time confirmation naming destination, content/data/files,
  price/terms if any, and consequence.

## Source Code Map

Primary host:

- `anima/server.py` - stdlib `ThreadingHTTPServer`, routes, auth gates, web surfaces, JSON endpoints,
  turn entrypoints, deploy stamp.
- `anima/web/` - 37 browser surfaces, including chat, security, privacy, learning, observation,
  founder, chairman, revenue, trust, resources, pipeline, and related dashboards.

Truth / visibility / verification:

- `anima/truth/` - append-only Truth Ledger, memory-language guard, query/fold/trace semantics.
- `anima/observation/` - trace-linked observation events and governance snapshots.
- `anima/verification/` - cert result spine, route registry, Diamond gate inputs.
- `scripts/run_master_cert_stack.py` - full cert stack.
- `scripts/run_diamond_v2.py` - three-run repeatability Diamond gate.

Security / vault / privacy:

- `anima/crypto.py` - encryption/key source helpers.
- `anima/secure_store.py` - private JSON/text/bytes/JSONL persistence substrate.
- `anima/vault_keys.py` - recovery-code and key-rotation lifecycle.
- `anima/vault_backup.py` - encrypted off-device vault bundles.
- `anima/passkey.py` - WebAuthn/passkey verifier.
- `anima/privacy_receipts.py` - turn/egress receipt ledger.
- `scripts/certify_vault_requires_encryption.py`
- `scripts/certify_vault_key_lifecycle.py`
- `scripts/certify_secure_store_no_plaintext.py`
- `scripts/certify_private_write_classification.py`
- `scripts/certify_encrypted_backup_restore.py`
- `scripts/certify_browser_origin_csrf.py`
- `scripts/certify_browser_session_cookies.py`
- `scripts/certify_browser_shell_replay_migration.py`

Memory / learning / cognition:

- `anima/memory_lirf.py` - durable personal facts and retraction behavior.
- `anima/spine.py` - deterministic known-fact and truth-preserving seams.
- `anima/mouth.py` - persona/dials/model response assembly and final gates.
- `anima/organs/` - routing, verifier, identity and cognitive organs.
- `anima/teaching/`
- `anima/auto_learn/`
- `anima/knowledge_packs/`
- `anima/self_evolution/`
- `anima/identity_health/`
- `anima/consent/`

Company / optional commercial modules:

- `anima/company/`
- `anima/company_operator/`
- `anima/collatio/`
- `anima/teams/`
- `anima/foundry/`
- `anima/commercial/`
- `anima/market_vision/`
- `anima/workforce/`
- `anima/revenue/`
- `anima/revenue_swarm/`
- `anima/distribution/`
- `anima/trust/`
- `anima/resources/`
- `anima/empire/`
- `anima/marketplaces/`

These commercial modules are optional/domain-pack work, not blockers for core Vera memory, voice,
vault, first-launch, or local companion behavior.

## Important Web Surfaces

- `/` - main chat
- `/security` - security posture and Vault controls
- `/privacy` - privacy receipts / flight recorder
- `/learning` - memory, corrections, unsupported, teaching, auto-learn visibility
- `/observation` - observation/event visibility
- `/verification` - certification/release visibility
- `/founder` - founder command center
- `/chairman` - chairman/capital dashboard
- `/self` - self/cognition dashboard
- `/trust/moat` - trust/proof/reputation moat
- `/resources` - resources and growth bottlenecks
- `/pipeline` - optional Upwork pipeline module
- `/revenue/cash` - optional revenue cash milestone module

## Latest Certified Slice

Tracked note:

```text
docs/vera/vera_certified_slice_2026_06_25.md
```

What closed:

- Vault key lifecycle remains certified in the master stack.
- `/founder` first-paints `Founder decisions needed` before slower briefing data arrives.
- `/trust/moat` first-paints governance/human-only/Observed visibility.
- Dirty-file reporting preserves exact `git status --short` paths.
- Unsupported memory count now uses folded ledger state.
- Live memory cert proves unsupported phrases do not ship, while caught draft attempts are trace-linked
  and closed by append-only retraction events.
- Operator rover includes governance visibility in per-route `ok` rows across 23 surfaces.
- Expose-auth cert is isolated from model/Whisper warm-up with `ANIMA_SKIP_WARM=1`.
- Desktop launcher is repo-generated and polish-certified.

## Vault / Key Lifecycle Must Be Preserved

Do not regress:

- first-run key posture
- no recovery material landing in plaintext
- display-once recovery codes
- salted-hash-only recovery storage
- one-time recovery-code consumption
- dry-run key rotation
- confirmed key rotation
- wrong-key refusal before mutation
- post-rotation new-key verification
- Security -> Vault UI visibility/actions
- no plaintext leakage in private ledgers, queues, staging, exports, or backups when a vault key is active

## Sensitive Data Rules

Treat these as sensitive:

- `.anima/`
- `.anima/Vera.*`
- Vera memory/history/personality/identity files
- browser sessions
- credentials
- tax IDs
- legal identifiers
- raw portal secrets
- sensitive account artifacts
- API keys
- private export/training bundles unless encrypted/sanitized

Do not push raw `.anima` state to GitHub or any new repo.

Use encrypted or sanitized snapshots only if continuity requires it.

## Backup / Handoff Locations

Older Vera handoff:

```text
/Volumes/LaCie-Storage/Desktop-Folders/LBackup/2026-06-23-vera-new-mac-handoff
```

This predates the current certified slice. Its recorded handoff commit was `aae7fd1`, while current
Vera `origin/anima` is ahead at `09e5daf`.

Migration backup to verify:

```text
/Volumes/LaCie-Storage/Desktop-Folders/LBackup/2026-06-22-codex-migration
```

Do not delete LaCie backup material.

## GitHub Wiring

Use GitHub issues/milestones/projects on:

```text
slmichaelj-beep/collatiolabs.com
```

Suggested labels:

- `vera`
- `security`
- `vault`
- `cert`
- `release-gate`
- `first-launch`
- `memory-truth`
- `product-mode`

Only create `slmichaelj-beep/vera-continuity` if it is explicitly a private sanitized handoff/mirror,
not the canonical implementation repo.

## Literal One-File Source Archive

This Markdown file is the human handoff. If Lamar needs the literal source code as one file, create a
sanitized tracked-source archive from Git, not from the working directory, so `.anima/` and ignored
private state are excluded:

```bash
cd /Users/lamarmichael/Developer/collatiolabs.com
git archive --format=tar.gz \
  --output=/Users/lamarmichael/Desktop/vera_source_$(git rev-parse --short HEAD).tar.gz \
  HEAD
```

That archive contains tracked source/docs only. It does not contain raw Vera memory/history/personality
runtime state.

## Current Work Posture

Vera is active software, not a greenfield setup. Before changing it:

- read `SYSTEM_OVERVIEW.md`
- read this file
- read `docs/vera/vera_weakness_register.md`
- inspect `git status --short --branch`
- inspect recent commits
- preserve verified marks
- preserve sensitive-state boundaries
- keep commercial/revenue modules optional unless Lamar explicitly asks to work on them

When in doubt, prefer small certified slices over broad refactors.
