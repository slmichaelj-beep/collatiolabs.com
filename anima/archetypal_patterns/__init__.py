"""archetypal_patterns — Layer 7 of the Human Operating Layer: the Archetypal Pattern Registry.

Jung's archetypes used as a SYMBOLIC PATTERN LANGUAGE for repeated PRODUCT / SYSTEM dynamics — never a
tool to diagnose the USER. This layer recognises recurring shapes in Vera's own behaviour (a hostile
catch that must be seen-not-absorbed = shadow; a confusing loop / contradictory UI = trickster; a state
transition that needs consent + proof + rollback = threshold/rite) and surfaces them as HYPOTHESES for
product action, with provenance and a non-diagnostic disclaimer.

Hard anti-overreach law (enforced by certify_archetype_no_user_diagnosis): never label the USER with an
archetype, never infer psychology from one event, never store an archetypal claim as a personal fact,
require REPEATED evidence + provenance, and present every pattern as a hypothesis about the SYSTEM —
not a diagnosis of the person.
"""
from . import schema, detector, policy  # noqa: F401
