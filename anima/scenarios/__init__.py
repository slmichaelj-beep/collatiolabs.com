"""scenarios — the Vera Total Reality Test foundation (Level 0): the finite user-space model.

inventory.py discovers the REAL product (surfaces / controls / routes / feature contracts); generator.py
reduces the infinite phrasing space into finite behaviour CLASSES and emits the Total Scenario Matrix;
schema.py is the axes + the machine-readable Scenario object. No invented surfaces, no invented controls.
"""
from . import schema, inventory, generator  # noqa: F401
