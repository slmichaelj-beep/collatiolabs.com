"""renegade — the integrated MONSTER test (Total Reality Level 7).

Where the Rover walks the house room by room, Renegade runs cross-subsystem ATTACK CHAINS (section 19):
hostile material + a source chip + a correction + host pressure + a model fallback + a prior contaminated
output + a consent boundary + the final gate + recovery. Each chain must HOLD; the harness proves it can
detect a breach (teeth). chains.py composes the REAL defenses; runner.py runs + classifies them.
"""
from . import chains, runner  # noqa: F401
